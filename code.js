switch(ops) {
    case '+':
        total = eval(fstOp.concat(ops,secOp));
    break
    case '-':
        total = eval(fstOp.concat(ops,secOp));
    break
    case '*':
        total = eval(fstOp.concat(ops,secOp));
    break
    case '/':
        total = eval(fstOp.concat(ops,secOp));
    break
}